{
  "type": "commonjs",
  "name": "meetingbot",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd src/example-app && npm install --legacy-peer-deps && npm run build",
    "typecheck": "pnpm --filter \"./src/**\" run typecheck"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "^18.3.3"
    }
  }
} 